const HEDERA_CREDENTIALS_NOT_FOUND = "Unable to load hedera credentials";
const default_maximum_transaction_fee = 100;
const maximum_payment_for_queries = 50;
const account_creation_initial_balance = 1000;

module.exports = {
    HEDERA_CREDENTIALS_NOT_FOUND,
    default_maximum_transaction_fee,
    maximum_payment_for_queries,
    account_creation_initial_balance,
};
